<!DOCTYPE html>
<head>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 80%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 10;
        padding: 0;
      }
    </style>
    <script src="/routes.js"></script>
</head>

<body>
      <h3><%= @route.title %></h3>
      <p><%= @route.description %></p>
      <%= button_to "Add a Review", reviews_path(), :class => "button medium", :method => :get %>
      <%= link_to "Edit this route", edit_route_path(@route.id), class: 'button' %>
      <div id="map"></div>
      <script type="text/javascript">
          var json_loc = JSON.parse('<%= raw(escape_javascript(@locations)) %>');
      </script>
      <script src="https://maps.googleapis.com/maps/api/js?libraries=places&key=AIzaSyBXzcgwL4SR4Pub-rXaTbGifuux-QbKoEA&callback=initMap" async defer></script>

</body>
</html>
